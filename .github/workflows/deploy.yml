name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

# Frontend is deployed automatically via Vercel's native GitHub integration.
# Backend is deployed automatically via Railway's native GitHub integration.
