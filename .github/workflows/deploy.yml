name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

# Frontend deploys automatically via Vercel's native GitHub integration.
# Backend deploys automatically via Railway's native GitHub integration.

jobs:
  info:
    name: Deployments handled by Vercel and Railway
    runs-on: ubuntu-latest
    steps:
      - run: echo "Frontend -> Vercel | Backend -> Railway"
